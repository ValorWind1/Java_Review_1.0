package java_tutorial;

public class logical_operators {
    public static void main(String[] args) {

    int boy, girl;
    boy = 18;
    girl = 99;

    if(boy > 10 || girl <60 ){        // && = either test in either side has to be true in order to be true. (AND)
                                      // || = Only one side has to be true in order for it to be true. (OR)

        System.out.println(" you can enter ");

    }else{

        System.out.println("You cannot enter ");
    }

    }
}
